# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (bevatten de DATABASE_URL/secrets — niet in git)
/.env
.env*.local

# prisma — lokale SQLite database (legacy)
/prisma/*.db
/prisma/*.db-journal

# typescript
*.tsbuildinfo

# Claude Code local settings
.claude/settings.local.json
