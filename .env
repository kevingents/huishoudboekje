# Niet-secret: lokale SQLite-database (door Prisma gelezen).
# Voor deploy/multi-device: vervang door een Postgres-URL en zet de provider
# in prisma/schema.prisma op "postgresql".
DATABASE_URL="file:./dev.db"
